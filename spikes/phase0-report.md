# flakeproof phase 0 report

Generated by `spikes/run-phase0.js`.

## Confusion matrix

| label \ verdict | cosmetic | semantic | unclear |
|---|---|---|---|
| cosmetic | 2 | 0 | 19 |
| semantic | 0 | 10 | 6 |

Total cases: 37 (live: 12), skipped: 7

## Misclassifications (success criterion: 0)

None.

## Unclear cases

- wrap-element at `#cta` (cosmetic): no signals
- wrap-element at `li.css-1a2b3c` (cosmetic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- add-class at `#cta` (cosmetic): no signals
- add-class at `header > a.btn` (cosmetic): no signals
- add-class at `#main-nav > li:nth-child(2) > a` (cosmetic): no signals
- add-class at `li.css-1a2b3c` (cosmetic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- rename-hashed-class at `li.css-1a2b3c` (cosmetic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- add-framework-attr at `#cta` (cosmetic): no signals
- add-framework-attr at `header > a.btn` (cosmetic): no signals
- add-framework-attr at `#main-nav > li:nth-child(2) > a` (cosmetic): no signals
- add-framework-attr at `li.css-1a2b3c` (cosmetic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- move-to-end at `li.css-1a2b3c` (cosmetic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- change-text at `li.css-1a2b3c` (semantic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- remove-element at `li.css-1a2b3c` (semantic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- wrap-element at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- wrap-element at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no element matched <a> with enough confidence, but other <a> elements remain; cannot tell a rename/move from a removal
- add-class at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- add-class at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no element matched <a> with enough confidence, but other <a> elements remain; cannot tell a rename/move from a removal
- add-framework-attr at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): no signals
- add-framework-attr at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no element matched <a> with enough confidence, but other <a> elements remain; cannot tell a rename/move from a removal
- move-to-end at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- change-text at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (semantic): no element matched <a> with enough confidence, but other <a> elements remain; cannot tell a rename/move from a removal
- change-href at `ul#menu-main-navigation > li:nth-child(1) > a` (semantic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- remove-element at `ul#menu-main-navigation > li:nth-child(1) > a` (semantic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- remove-element at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (semantic): no element matched <a> with enough confidence, but other <a> elements remain; cannot tell a rename/move from a removal

## Skipped

- rename-hashed-class at `#cta`: mutation not applicable
- rename-hashed-class at `header > a.btn`: mutation not applicable
- rename-hashed-class at `#main-nav > li:nth-child(2) > a`: mutation not applicable
- move-to-end at `#cta`: mutation not applicable
- move-to-end at `header > a.btn`: mutation not applicable
- move-to-end at `#main-nav > li:nth-child(2) > a`: mutation not applicable
- change-href at `li.css-1a2b3c`: mutation not applicable
