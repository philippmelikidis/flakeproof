# flakeproof phase 0 report

Generated by `spikes/run-phase0.js`.

## Confusion matrix

| label \ verdict | cosmetic | semantic | unclear |
|---|---|---|---|
| cosmetic | 3 | 0 | 18 |
| semantic | 0 | 11 | 5 |

Total cases: 37 (live: 12), skipped: 11

## Misclassifications (success criterion: 0)

None.

## Unclear cases

- wrap-element at `#cta` (cosmetic): no signals
- wrap-element at `header > a.btn` (cosmetic): element depth changed (wrapper inserted/removed) and selector depends on structure; a sibling of the anchor disappeared; cannot tell a rename from a removal
- wrap-element at `li.css-1a2b3c` (cosmetic): no signals
- add-class at `#cta` (cosmetic): no signals
- add-class at `header > a.btn` (cosmetic): no signals
- add-class at `#main-nav > li:nth-child(2) > a` (cosmetic): no signals
- add-class at `li.css-1a2b3c` (cosmetic): no signals
- add-framework-attr at `#cta` (cosmetic): no signals
- add-framework-attr at `header > a.btn` (cosmetic): no signals
- add-framework-attr at `#main-nav > li:nth-child(2) > a` (cosmetic): no signals
- add-framework-attr at `li.css-1a2b3c` (cosmetic): no signals
- move-to-end at `li.css-1a2b3c` (cosmetic): no signals
- remove-element at `li.css-1a2b3c` (semantic): no element matched <li> with enough confidence, but other <li> elements remain; cannot tell a rename/move from a removal
- wrap-element at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): element depth changed (wrapper inserted/removed) and selector depends on structure; a sibling of the anchor disappeared; cannot tell a rename from a removal
- wrap-element at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no signals
- add-class at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): no signals
- add-class at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no signals
- add-framework-attr at `ul#menu-main-navigation > li:nth-child(1) > a` (cosmetic): no signals
- add-framework-attr at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (cosmetic): no signals
- change-text at `ul#menu-main-navigation > li:nth-child(1) > a` (semantic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- change-text at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (semantic): selector relies on class ".fusion-builder-row-1" which an ancestor lost; cannot tell a rename from a real state change
- remove-element at `ul#menu-main-navigation > li:nth-child(1) > a` (semantic): element depth changed (wrapper inserted/removed) and selector depends on structure; selector relies on id "#menu-main-navigation" which an ancestor lost; cannot tell a rename from a real state change
- remove-element at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact` (semantic): selector relies on class ".fusion-builder-row-1" which an ancestor lost; cannot tell a rename from a real state change

## Skipped

- rename-hashed-class at `#cta`: mutation not applicable
- rename-hashed-class at `header > a.btn`: mutation not applicable
- rename-hashed-class at `#main-nav > li:nth-child(2) > a`: mutation not applicable
- move-to-end at `#cta`: mutation not applicable
- move-to-end at `header > a.btn`: mutation not applicable
- move-to-end at `#main-nav > li:nth-child(2) > a`: mutation not applicable
- change-href at `li.css-1a2b3c`: mutation not applicable
- rename-hashed-class at `ul#menu-main-navigation > li:nth-child(1) > a`: mutation not applicable
- rename-hashed-class at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact`: mutation not applicable
- move-to-end at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact`: mutation not applicable
- change-href at `.fusion-tb-header .fusion-builder-row-1 a.fusion-button.open-contact`: mutation not applicable

## Checkpoint assessment

Filled in by hand after the measurement run, per the phase 0 plan.

- [x] Spike 1 (anchor extraction): proven. extractAnchor handles all four captured Playwright error formats plus Robot Framework output.xml, all validated against genuinely captured fixtures, including selectors containing quotes and anchorless assertion failures.
- [x] Spike 2 (classifier): success criterion met. 0 misclassifications across 37 fixture and live cases; every cosmetic-vs-semantic verdict that was given was correct.
- [x] Unclear rate documented and explained. 25 of 37 cases abstained, from two causes: mutations that leave no selector-relevant or meaning-bearing trace (no signals), and weak-identity elements (no id, no own text, no accessible name, no href) that the matcher cannot re-identify with confidence. Live capture confirmed weak-identity elements are a real category on production sites, not a fixture artifact.

Decision: GO for phase 1.

The abstention rate is the main quality lever to work on, not a blocker: the classifier never guessed wrong, it declined to answer. Three items carry over into phase 1 planning:

1. Matcher improvements for weak-identity elements, for example contextual scoring that includes ancestor and child evidence, so bare li/div elements can be re-identified.
2. Additional evidence sources for the no-signals cases, so more cosmetic mutations produce a positive cosmetic verdict instead of an abstention.
3. A locality term in the matcher (path distance or lowest common ancestor depth), so duplicated DOM regions such as light/dark header variants cannot capture the match. The final review showed the matcher twice locked onto the pristine twin of a mutated element on the live site, which neutralized the semantic evidence; both cases were saved from misclassification only by the ambiguous-evidence gate on the ancestor id. Anchor-selection guidance helps but does not fix the underlying gap.
